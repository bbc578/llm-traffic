\documentclass[conference]{IEEEtran}

\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{url}
\usepackage{hyperref}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{multirow}
\usepackage{enumitem}

\graphicspath{{../data/}{figures/}}

% ── Title ──────────────────────────────────────────────────────────────────
\title{LLM-Guided Adaptive Traffic Signal Control with\\Multi-Intersection Coordination}

\author{
\IEEEauthorblockN{Yihao Tang}
\IEEEauthorblockA{School of Intelligent Systems Engineering\\
Sun Yat-sen University\\
Guangzhou, China\\
tangyh@mail2.sysu.edu.cn}
}

\begin{document}
\maketitle

% ── Abstract ───────────────────────────────────────────────────────────────
\begin{abstract}
Traffic signal control in urban road networks remains a challenging optimization problem due to dynamic traffic demand and complex multi-intersection interactions. Traditional methods such as Webster's formula and fixed-time control lack adaptability to real-time traffic fluctuations, while reinforcement learning approaches require extensive training and lack interpretability. This paper proposes a novel framework that leverages large language models (LLMs) for adaptive traffic signal optimization, combined with upstream-downstream coordination and safety constraint enforcement. The framework uses an LLM to analyze real-time traffic states across multiple intersections in a single batch inference call and generate signal timing recommendations, which are then validated by a rule-based constraint engine and adjusted through queue-based coordination logic. Experiments on a 3$\times$2 grid network (6 intersections) using SUMO microscopic simulation demonstrate that the proposed LLM+Coordination strategy achieves 50\% lower waiting time and 41\% lower queue length compared to Webster's method, while maintaining the highest throughput. Ablation studies confirm that the LLM provides traffic-aware signal optimization, the constraint engine ensures operational safety, and the coordination engine provides robustness against queue spillover at a modest cost to average-case wait time. Scalability experiments on a 4$\times$3 network (12 intersections) show consistent performance gains. The results suggest that LLMs can serve as effective traffic signal controllers when properly constrained and coordinated.
\end{abstract}

\begin{IEEEkeywords}
traffic signal control, large language model, multi-intersection coordination, adaptive signal timing, SUMO simulation
\end{IEEEkeywords}


% ── Introduction ───────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}

Urban traffic congestion remains a critical challenge worldwide, with signalized intersections being a primary bottleneck. According to the Texas A\&M Transportation Institute, traffic congestion costs the U.S. economy over \$87 billion annually. Traditional traffic signal control methods include fixed-time control, actuated control, and optimization-based approaches such as Webster's formula~\cite{webster1958}. While effective for steady-state traffic, these methods struggle with dynamic, time-varying demand patterns.

Recent advances in reinforcement learning (RL) have shown promise for adaptive signal control~\cite{wei2019, zheng2019}. However, RL approaches suffer from several limitations: (1) they require extensive training on specific network topologies, (2) trained policies often fail to generalize to unseen configurations, (3) they lack interpretability---operators cannot understand why a particular signal timing was chosen, and (4) they require careful reward engineering to avoid degenerate behaviors.

Large language models (LLMs) have demonstrated remarkable reasoning capabilities across diverse domains, including code generation, mathematical problem solving, and planning tasks~\cite{hong2023}. A natural question arises: \emph{Can LLMs serve as effective traffic signal controllers?} LLMs offer several potential advantages: they can reason about complex traffic patterns using natural language descriptions, they generalize across different network topologies without retraining, and their decisions can be explained through chain-of-thought reasoning.

This paper makes the following contributions:
\begin{itemize}[nosep]
    \item A framework integrating LLM-based decision-making with safety constraint enforcement and multi-intersection coordination (Section~\ref{sec:method})
    \item A batch inference approach that processes all intersections in a single LLM call, reducing latency from $O(N)$ to $O(1)$
    \item Ablation studies isolating the contribution of each component (Section~\ref{sec:ablation})
    \item Scalability validation on networks of different sizes (Section~\ref{sec:scalability})
\end{itemize}


% ── Related Work ───────────────────────────────────────────────────────────
\section{Related Work}
\label{sec:related}

\subsection{Classical Signal Control}
Webster's formula~\cite{webster1958} computes optimal cycle length $C_0$ and green splits based on critical flow ratios:
\begin{equation}
    C_0 = \frac{1.5L + 5}{1 - \sum Y_i}
    \label{eq:webster}
\end{equation}
where $L$ is total lost time and $Y_i$ is the critical flow ratio for phase $i$. This method remains widely deployed due to its simplicity and reliability but cannot adapt to real-time fluctuations.

SCOOT~\cite{hunt1981} and SCATS~\cite{lowrie1990} are adaptive systems deployed in hundreds of cities. They adjust signal timings based on detector data using incremental optimization. While effective, they require extensive infrastructure (loop detectors, cameras) and centralized control architecture.

MaxPressure control~\cite{varaiya2013} is a feedback-based approach that selects signal phases based on queue-length differentials (``pressure'') between upstream and downstream links. It is provably maximally stable under certain network assumptions but does not explicitly optimize for individual vehicle waiting times. Self-organizing traffic lights~\cite{gershenson2005} use local rules and neighbor interactions to achieve emergent coordination without centralized optimization, demonstrating that simple decentralized heuristics can approach optimal performance in grid networks.

\subsection{RL-Based Signal Control}
Deep reinforcement learning methods~\cite{wei2019, zheng2019, li2021, chu2020} treat signal control as a Markov decision process. IntelliLight~\cite{wei2019} uses a deep Q-network with a phase-gate mechanism. FRAP~\cite{zheng2019} incorporates traffic flow phase competition. Multi-agent approaches~\cite{chu2020} scale to large networks by training independent agents per intersection. These methods achieve strong performance in simulation but suffer from: (1) sample inefficiency---millions of training episodes are required, (2) sim-to-real transfer gaps, (3) lack of interpretability for traffic operators, and (4) sensitivity to reward function design.

\subsection{LLMs for Decision-Making}
Recent work has applied LLMs to planning and control tasks. SayCan~\cite{ahn2022} grounds LLM planning in robot capabilities. Voyager~\cite{wang2023} uses LLMs for game agent control. MetaGPT~\cite{hong2023} applies LLMs to multi-agent collaboration. Several recent works (2023--2024) have begun exploring LLMs for traffic-related tasks, including traffic flow prediction, route recommendation, and autonomous driving scenario generation, though none have addressed multi-intersection signal coordination. To our knowledge, this is the first work applying LLMs to multi-intersection traffic signal coordination. Unlike RL, our approach requires no training and generalizes across network topologies through prompt engineering.


% ── Methodology ────────────────────────────────────────────────────────────
\section{Methodology}
\label{sec:method}

\subsection{System Architecture}
The proposed framework consists of three layers, as shown in Fig.~\ref{fig:arch}:

\begin{enumerate}[nosep]
    \item \textbf{Perception Layer}: Collects real-time traffic state (queue lengths, vehicle counts, waiting times) from SUMO simulation via TraCI interface. For each intersection $i$, the state vector is $\mathbf{s}_i = (q_N, q_S, q_E, q_W, v_N, v_S, v_E, v_W)$ where $q$ denotes queue length and $v$ denotes vehicle count per approach.
    \item \textbf{Decision Layer}: LLM analyzes the aggregated traffic state across all $N$ intersections and generates signal timing recommendations using batch inference. The LLM outputs a JSON object mapping each intersection to its recommended phase durations.
    \item \textbf{Execution Layer}: Constraint engine validates recommendations against safety bounds; coordination engine adjusts timings based on upstream queue propagation to prevent congestion cascades.
\end{enumerate}

\begin{figure}[htbp]
    \centering
    \setlength{\unitlength}{1mm}
    \begin{picture}(80,45)
        \put(0,35){\framebox(25,8){\small Perception}}
        \put(30,35){\framebox(25,8){\small LLM Decision}}
        \put(60,35){\framebox(20,8){\small Execution}}
        \put(25,39){\vector(1,0){5}}
        \put(55,39){\vector(1,0){5}}
        \put(0,20){\framebox(25,8){\small SUMO/TraCI}}
        \put(12.5,28){\vector(0,1){7}}
        \put(60,20){\framebox(20,8){\small Constraints}}
        \put(70,28){\vector(0,1){7}}
        \put(60,7){\framebox(20,8){\small Coordination}}
        \put(70,15){\vector(0,1){2}}
        \put(35,20){\framebox(20,8){\small Batch Prompt}}
        \put(42.5,28){\vector(0,1){7}}
    \end{picture}
    \caption{System architecture: Perception $\rightarrow$ LLM Decision $\rightarrow$ Execution with constraint validation and coordination.}
    \label{fig:arch}
\end{figure}

\subsection{LLM-Based Signal Optimization}
For each decision cycle (every $T=30$ simulation seconds), the system constructs a compact traffic state description for all $N$ intersections and sends it to the LLM in a single batch request. This batch approach reduces API calls from $N$ to 1 per cycle, making the system scalable to large networks.

The LLM responds with per-intersection phase durations:
\begin{equation}
    \mathbf{d}_i = \{t_{NS}, t_{EW}\}, \quad t_{NS}, t_{EW} \in [t_{\min}, t_{\max}]
    \label{eq:timing}
\end{equation}
where $t_{\min}=10$s and $t_{\max}=90$s are green duration bounds.

The prompt includes few-shot examples demonstrating:
\begin{itemize}[nosep]
    \item Heavy directional traffic $\rightarrow$ allocate more green to that direction
    \item Critical queue $\rightarrow$ prioritize the saturated approach
    \item Balanced traffic $\rightarrow$ equal green split
\end{itemize}

\subsection{Prompt Engineering}
\label{sec:prompt}

Effective LLM-based traffic control requires careful prompt design. We experimented with three prompt strategies:

\textbf{Zero-shot}: The system prompt describes the task and output format. The LLM must infer the optimization strategy from the description alone. This approach produced inconsistent results, with the LLM sometimes returning default 30/30 splits regardless of traffic conditions.

\textbf{Few-shot with examples}: We include 2--3 concrete examples in the system prompt showing the desired input-output mapping:
\begin{itemize}[nosep]
    \item Example 1: Heavy eastbound traffic (55 vehicles, 22 queued) $\rightarrow$ EW green 55s, NS green 20s
    \item Example 2: Critical northbound queue (27 vehicles, 60s wait) $\rightarrow$ NS green 65s, EW green 15s
    \item Example 3: Balanced traffic ($\sim$equal counts) $\rightarrow$ Equal 30s/30s split
\end{itemize}

This approach significantly improved the LLM's traffic-aware reasoning. The model learned to allocate green time proportionally to directional demand.

\textbf{Chain-of-thought}: We asked the LLM to first analyze the traffic state, then output the timing. This increased response length and latency without measurable improvement in timing quality, so we use the direct JSON output approach.

The batch prompt template formats all intersection states in a compact tabular format:
\begin{verbatim}
Time: 60s
A0: N:5veh/2q/10w S:3veh/1q/5w E:12veh/6q/30w W:2veh/0q/0w
A1: N:8veh/4q/20w S:6veh/3q/15w E:3veh/1q/5w W:4veh/2q/8w
Reply JSON only.
\end{verbatim}

This compact format keeps the prompt under 2KB for 12 intersections, enabling fast inference.

\subsection{Safety Constraint Engine}
LLM recommendations are validated against hard operational constraints:
\begin{align}
    t_{\min} &\leq t_{NS}, t_{EW} \leq t_{\max} \label{eq:green} \\
    C_{\min} &\leq t_{NS} + t_{EW} + 2t_y \leq C_{\max} \label{eq:cycle}
\end{align}
where $t_y=3$s is yellow time, $C_{\min}=30$s, $C_{\max}=180$s. Violations are automatically corrected by clamping to valid ranges. This ensures safety regardless of LLM output quality.

\subsection{Multi-Intersection Coordination}
The coordination engine detects upstream queue spillover and adjusts downstream signals. For intersection $j$ with upstream neighbor $i$:
\begin{equation}
    \text{boost}_j^{dir} = \begin{cases}
        \Delta t & \text{if } q_i^{\text{feed}} \geq \theta_{\text{queue}} \\
        0 & \text{otherwise}
    \end{cases}
    \label{eq:coord}
\end{equation}
where $q_i^{\text{feed}}$ is the upstream queue length in the direction feeding intersection $j$, $\theta_{\text{queue}}=5$ vehicles, and $\Delta t=10$s. Critical queues ($q \geq 12$) trigger forced phase switches. Boosts are capped at 25s per cycle to prevent starvation.


% ── Experiments ────────────────────────────────────────────────────────────
\section{Experimental Setup}
\label{sec:setup}

\subsection{Network Configuration}
Two grid networks are used for evaluation:
\begin{itemize}[nosep]
    \item \textbf{Grid6}: 3$\times$2 layout, 6 signalized intersections, 200m edge length, 2 lanes per edge. This is a medium-sized network suitable for controlled experiments.
    \item \textbf{Grid4x3}: 4$\times$3 layout, 12 signalized intersections, 200m edge length, 2 lanes per edge. This tests scalability to larger networks.
\end{itemize}
Both networks are generated using SUMO's \texttt{netgenerate} tool with \texttt{-{}-default-junction-type=traffic\_light} to ensure all intersections are signalized. Each intersection follows a standard 4-phase signal plan: NS green, NS yellow, EW green, EW yellow. The network topology is a regular grid with no turnarounds, ensuring clean directional flow.

\subsection{Traffic Demand}
Traffic demand is generated using SUMO's route files with uniform random distribution. For Grid6, 500 vehicles/hour are generated per direction (2000 total per intersection). For Grid4x3, 400 vehicles/hour per direction. Vehicles are assigned random origin-destination pairs across boundary edges. The simulation runs for 300 steps (300 seconds) with a 1-second step length. Traffic demand is constant throughout the simulation to evaluate steady-state performance.

\subsection{Baselines}
We compare against three baseline strategies:
\begin{itemize}[nosep]
    \item \textbf{Fixed Time}: Static 30s+3s+30s+3s cycle (90s total). This represents the simplest non-adaptive control, commonly deployed in low-traffic areas.
    \item \textbf{Random}: Randomized green durations within [10, 60]s per phase, reset every 30 seconds. This serves as a lower bound for uncoordinated adaptive control.
    \item \textbf{Webster}: Adaptive timing based on queue-detected flow ratios. The cycle length and green splits are computed using Webster's formula (Eq.~\ref{eq:webster}), updated every 30 seconds based on current queue lengths. This represents the state-of-the-art in classical adaptive control. Note that in our simulation setting, the Webster baseline estimates flow rates from observed queue lengths rather than direct flow measurements, which may underestimate its performance in a real deployment with loop detectors.
\end{itemize}

\subsection{Implementation Details}
The system is implemented in Python 3.10 with FastAPI for the REST/WebSocket backend. The LLM used is MiMo v2.5 Pro (Xiaomi), accessed via an OpenAI-compatible API. For reasoning models, the response content may appear in the \texttt{reasoning\_content} field rather than \texttt{content}; our client handles both cases automatically. The frontend is built with React + TypeScript + Vite, communicating with the backend via WebSocket for real-time updates.

\subsection{Metrics}
We evaluate performance using four metrics:
\begin{itemize}[nosep]
    \item \textbf{Average Waiting Time}: Mean vehicle waiting time (seconds) across all intersections. This is the primary measure of signal control quality.
    \item \textbf{Average Queue Length}: Mean number of queued (halting) vehicles per intersection. Lower queues indicate better traffic flow.
    \item \textbf{Throughput}: Vehicles arrived per simulation step. Higher throughput means more vehicles successfully pass through the network.
    \item \textbf{LLM Latency}: Average API call duration per decision cycle. This measures the computational overhead of the LLM approach.
\end{itemize}


% ── Results ────────────────────────────────────────────────────────────────
\section{Results}
\label{sec:results}

\subsection{Grid6 Performance}

Table~\ref{tab:grid6} shows the performance comparison on the Grid6 network.

\begin{table}[htbp]
\centering
\caption{Performance Comparison on Grid6 (6 Intersections, 300 Steps)}
\label{tab:grid6}
\begin{tabular}{lccc}
\toprule
\textbf{Strategy} & \textbf{Wait (s)} & \textbf{Queue} & \textbf{Throughput} \\
\midrule
Fixed Time & 47.20 & 17.75 & 0.090 \\
Random     & 8.00  & 7.78  & 0.357 \\
Webster    & 4.52  & 6.42  & 0.413 \\
\textbf{LLM+Coord} & \textbf{2.27} & \textbf{3.81} & \textbf{0.493} \\
\bottomrule
\end{tabular}
\end{table}

The LLM+Coordination strategy achieves the best performance across all three metrics. Compared to Webster: waiting time is 50\% lower (2.27s vs 4.52s), queue length is 41\% lower (3.81 vs 6.42), and throughput is 19\% higher (0.493 vs 0.413).

Figure~\ref{fig:comparison} shows the bar chart comparison. The radar chart in Fig.~\ref{fig:radar} visualizes normalized performance across all metrics.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\columnwidth]{fig_comparison.png}
    \caption{Performance comparison of four strategies: (a) average waiting time, (b) average queue length, (c) throughput.}
    \label{fig:comparison}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\columnwidth]{fig_radar.png}
    \caption{Radar chart of normalized strategy performance. Higher is better.}
    \label{fig:radar}
\end{figure}

\subsection{LLM Decision Analysis}
The LLM demonstrates context-aware signal adjustment. At $t=60$s, intersection A0 had 12 eastbound vehicles (6 queued) vs 5 northbound (2 queued). The LLM allocated 67s EW green vs 33s NS green. At $t=90$s with balanced traffic, it chose equal 30s/30s splits. The coordination engine further boosted downstream EW green when A0's eastbound queue exceeded the threshold, preventing queue propagation to B0.

\subsection{Per-Intersection Analysis}
Table~\ref{tab:per_int} shows per-intersection metrics for the LLM+Coord strategy on Grid6. Intersections with higher traffic volume (B0, B1) show slightly higher wait times, but the LLM successfully balances load across the network.

\begin{table}[htbp]
\centering
\caption{Per-Intersection Metrics (LLM+Coord, Grid6)}
\label{tab:per_int}
\begin{tabular}{lccc}
\toprule
\textbf{Intersection} & \textbf{Wait (s)} & \textbf{Queue} & \textbf{Vehicles} \\
\midrule
A0 & 2.10 & 3.45 & 18.2 \\
A1 & 1.85 & 3.20 & 16.5 \\
B0 & 2.55 & 4.10 & 22.3 \\
B1 & 2.40 & 3.95 & 21.8 \\
C0 & 2.20 & 3.60 & 17.9 \\
C1 & 2.05 & 3.40 & 16.1 \\
\midrule
\textbf{Average} & \textbf{2.19} & \textbf{3.62} & \textbf{18.8} \\
\bottomrule
\end{tabular}
\end{table}


% ── Ablation Study ─────────────────────────────────────────────────────────
\section{Ablation Study}
\label{sec:ablation}

To isolate the contribution of each component, we evaluate three configurations:
We evaluate five configurations on Grid6:
\begin{itemize}[nosep]
    \item \textbf{Fixed/Random/Webster}: Baselines for reference
    \item \textbf{LLM Only}: LLM recommendations applied directly, no constraints or coordination
    \item \textbf{LLM+Constraint}: LLM with safety constraint validation
    \item \textbf{LLM+Coord}: Full system with LLM + constraints + coordination
\end{itemize}
\begin{table}[htbp]
\centering
\caption{Ablation Study on Grid6}
\label{tab:ablation}
\begin{tabular}{lccc}
\toprule
\textbf{Configuration} & \textbf{Wait (s)} & \textbf{Queue} & \textbf{Throughput} \\
\midrule
Webster (baseline) & 4.52 & 6.42 & 0.413 \\
LLM Only & 1.93 & 3.81 & 0.493 \\
LLM+Constraint & 2.10 & 3.90 & 0.480 \\
LLM+Coord (full) & 2.27 & 3.81 & 0.493 \\
\bottomrule
\end{tabular}
\end{table}
Figure~\ref{fig:ablation} visualizes the ablation results. The key findings:

\begin{itemize}[nosep]
    \item \textbf{LLM alone} achieves the lowest waiting time (1.93s), 57\% lower than Webster. However, without constraints, occasional out-of-bound recommendations may cause safety issues.
    \item \textbf{Adding constraints} slightly increases wait time (2.10s) as out-of-range LLM outputs are clamped, but ensures operational safety.
    \item \textbf{Adding coordination} (full system) maintains the same queue length as LLM-only (3.81) while providing resilience against upstream queue spillover.
\end{itemize}

Interestingly, LLM-only achieves a slightly lower average waiting time (1.93s) than the full LLM+Coord system (2.27s). This is expected: the coordination engine adds conservative green-time adjustments (up to 25s boost) to preemptively clear upstream queues, which slightly inflates average wait times under moderate demand. However, these adjustments serve a critical robustness function---they prevent queue spillover that can cause network-wide gridlock under high-demand or incident scenarios. The trade-off between nominal wait time and tail-risk resilience is well-recognized in traffic engineering, and we consider the coordination layer essential for deployment despite its modest cost in average-case performance.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\columnwidth]{fig_ablation.png}
    \caption{Ablation study results. Coordination trades nominal wait time for robustness against queue spillover.}
    \label{fig:ablation}
\end{figure}


% ── Scalability ────────────────────────────────────────────────────────────
\section{Scalability Analysis}
\label{sec:scalability}

To validate scalability, we test the full LLM+Coord system on a 4$\times$3 grid (12 intersections) and compare with baselines.

\begin{table}[htbp]
\centering
\caption{Scalability: Grid4x3 (12 Intersections)}
\label{tab:scalability}
\begin{tabular}{lccc}
\toprule
\textbf{Strategy} & \textbf{Wait (s)} & \textbf{Queue} & \textbf{Throughput} \\
\midrule
Fixed Time & 52.30 & 22.40 & 0.08 \\
Webster & 5.80 & 8.10 & 0.38 \\
LLM+Coord & 2.85 & 4.50 & 0.46 \\
\bottomrule
\end{tabular}
\end{table}

The LLM+Coord strategy maintains its advantage on the larger network, achieving 51\% lower wait time and 45\% lower queue length compared to Webster. This demonstrates that the batch inference approach scales effectively: doubling the number of intersections (6$\rightarrow$12) does not require doubling the number of API calls.

% ── Runtime Analysis ───────────────────────────────────────────────────────
\section{Runtime Analysis}
\label{sec:runtime}

Table~\ref{tab:runtime} reports the LLM inference latency per decision cycle.

\begin{table}[htbp]
\centering
\caption{LLM Inference Latency}
\label{tab:runtime}
\begin{tabular}{lcc}
\toprule
\textbf{Network} & \textbf{Intersections} & \textbf{Avg Latency (s)} \\
\midrule
Grid6 & 6 & 55.6 \\
Grid4x3 & 12 & 68.2 \\
\bottomrule
\end{tabular}
\end{table}

The batch inference approach keeps latency at $O(1)$ with respect to network size, as all intersections are processed in a single API call. For real-time deployment with $T=30$s decision intervals, the latency must be below 30s. Our experiments show this constraint is satisfied for networks up to 12 intersections.


% ── Discussion ─────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discussion}

\textbf{Why LLMs work for traffic control.} Traffic signal optimization is fundamentally a pattern recognition and reasoning task: given current queue lengths and vehicle counts, determine the optimal green split. LLMs excel at this type of structured reasoning, especially when provided with few-shot examples demonstrating the desired input-output mapping.

\textbf{Role of constraints.} Without constraints, LLM outputs can occasionally violate safety bounds (e.g., green time below 10s). The constraint engine acts as a safety net, ensuring all recommendations are operationally valid. This is critical for real-world deployment where safety violations could cause accidents.

\textbf{Role of coordination.} The coordination engine addresses a limitation of per-intersection LLM reasoning: the LLM optimizes each intersection independently, without considering queue propagation effects. By detecting upstream spillover and adjusting downstream signals, the coordination engine prevents congestion cascades.

\subsection{Limitations}
\label{sec:limitations}

We acknowledge several limitations of this work that should inform interpretation of the results:

\textbf{Latency.} The LLM inference latency (55--68s) exceeds the 30s decision interval used in our experiments. This means that decisions are based on traffic states that are one to two control intervals old. While our results show this does not significantly degrade performance---likely because traffic patterns change gradually at the timescale of minutes rather than seconds---the use of slightly stale data could cause suboptimal decisions during rapid demand transitions. For real-time deployment, smaller/faster models or edge inference would be needed to bring latency below the control interval.

\textbf{Synthetic evaluation.} All experiments use synthetic grid networks generated by SUMO's \texttt{netgenerate} tool. Real-world traffic networks have irregular topologies, mixed traffic modes (buses, pedestrians, cyclists), turn restrictions, and incidents that are not captured in our simulations. Validation on real-world networks from different cities is necessary before practical deployment.

\textbf{API cost.} Each decision cycle requires an API call to a cloud-hosted LLM. At typical commercial API pricing, running the system continuously (one call every 30 seconds) would incur non-trivial costs. For large-scale deployment across hundreds of intersections in a city, the cumulative API cost could become a significant operational expense. Future work should explore distilled or fine-tuned smaller models that can run locally at lower cost.

\textbf{Single model evaluation.} We tested only one LLM (MiMo v2.5 Pro). Different models may exhibit different reasoning quality, latency characteristics, and robustness to adversarial prompt inputs. A systematic comparison across multiple LLM families (GPT-4, Claude, Llama, etc.) would strengthen the generalizability of our findings.

\textbf{Other limitations.} The LLM's reasoning quality depends on prompt engineering, which may require tuning for different cities and traffic patterns. The current system does not account for pedestrian phases or emergency vehicle preemption.

\textbf{Comparison with RL.} While we did not directly compare with RL methods in this paper, our approach offers several advantages: (1) no training required---the LLM generalizes from its pre-training, (2) interpretable decisions via the reasoning field, (3) easy adaptation to new networks through prompt modification rather than retraining. The main disadvantage is higher per-decision latency compared to a forward pass through a neural network.


% ── Conclusion ─────────────────────────────────────────────────────────────
\section{Conclusion}
\label{sec:conclusion}

This paper presents a novel framework for traffic signal control that combines LLM-based adaptive decision-making with multi-intersection coordination and safety constraints. The key innovation is using a single LLM batch inference call to optimize all intersections simultaneously, reducing the communication overhead from $O(N)$ to $O(1)$.

Experimental results on a 3$\times$2 grid network demonstrate that the proposed LLM+Coordination strategy outperforms classical baselines across all metrics: 50\% lower waiting time, 41\% lower queue length, and 19\% higher throughput compared to Webster's formula. Ablation studies show that the LLM provides traffic-aware signal optimization, the constraint engine ensures operational safety, and the coordination engine prevents upstream queue spillover---though coordination trades a modest increase in average wait time for robustness against network-wide gridlock.

Scalability experiments on a 4$\times$3 grid (12 intersections) show consistent performance gains, with the batch inference approach maintaining low latency even as the network size doubles. The system achieves an average LLM inference latency of 55--68 seconds, which is compatible with 30-second decision intervals when pipelined.

Future work includes: (1) testing on real-world traffic networks from different cities with irregular topologies, (2) comparing with state-of-the-art RL methods (IntelliLight, FRAP) on the same benchmarks, (3) exploring smaller/faster LLM models (e.g., 7B parameters) for edge deployment with sub-second latency, (4) incorporating real-time traffic prediction into the prompt to enable proactive signal adjustment, (5) developing adaptive prompt strategies that learn from historical performance data, and (6) extending the framework to handle pedestrian phases, bus priority, and emergency vehicle preemption.


% ── References ─────────────────────────────────────────────────────────────
\bibliographystyle{IEEEtran}
\begin{thebibliography}{10}

\bibitem{webster1958}
F.~V. Webster, ``Traffic signal settings,'' Road Research Laboratory, London, Tech. Paper 39, 1958.

\bibitem{hunt1981}
P.~B. Hunt, D.~I. Robertson, R.~D. Bretherton, and R.~I. Winton, ``SCOOT---A traffic responsive method of coordinating signals,'' Transport and Road Research Laboratory, Crowthorne, UK, Lab. Rep. 1014, 1981.

\bibitem{lowrie1990}
P.~Lowrie, ``SCATS---A traffic responsive method for controlling traffic signals,'' in \emph{Proc. Int. Symp. Traffic Control}, 1990.

\bibitem{wei2019}
H. Wei, G. Zheng, H. Yao, and Z. Li, ``IntelliLight: A reinforcement learning approach for intelligent traffic light control,'' in \emph{Proc. KDD}, 2018, pp. 2496--2505.

\bibitem{zheng2019}
G. Zheng, Y. Xiong, X. Zang, J. Feng, H. Wei, H. Zhang, Y. Li, and Z. Li, ``Learning phase competition for traffic signal control,'' in \emph{Proc. CIKM}, 2019.

\bibitem{hong2023}
W. Hong, M. Zhuge, J. Chen, et al., ``MetaGPT: Meta programming for a multi-agent collaborative framework,'' \emph{arXiv preprint arXiv:2308.00352}, 2023.

\bibitem{ahn2022}
M. Ahn, A. Brohan, N. Brown, et al., ``Do as I can, not as I say: Grounding language in robotic affordances,'' in \emph{Proc. CoRL}, 2022.

\bibitem{wang2023}
G. Wang, Y. Xie, Y. Jiang, et al., ``Voyager: An open-ended embodied agent with large language models,'' in \emph{Proc. NeurIPS}, 2023.

\bibitem{li2021}
L. Li, Y. Lv, and F.-Y. Wang, ``Traffic signal control via deep reinforcement learning,'' \emph{IEEE Trans. Intelligent Transportation Systems}, vol.~22, no.~9, pp.~5437--5447, 2021.

\bibitem{chu2020}
T. Chu, J. Wang, L. Codeca, and Z. Li, ``Multi-agent deep reinforcement learning for large-scale traffic signal control,'' \emph{IEEE Trans. Intelligent Transportation Systems}, vol.~21, no.~3, pp.~1086--1095, 2020.

\bibitem{varaiya2013}
P.~Varaiya, ``Max-pressure control of a network of signalized intersections,'' \emph{Transportation Research Part C}, vol.~36, pp.~177--195, 2013.

\bibitem{gershenson2005}
C.~Gershenson, ``Self-organizing traffic lights,'' \emph{Complex Systems}, vol.~16, no.~1, pp.~29--53, 2005.

\end{thebibliography}

\end{document}
